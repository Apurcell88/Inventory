<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body>
    <%- include('./partials/nav.ejs') %>
    <div class="bg-black h-screen grid grid-cols-6">
        <%- include('./partials/aside.ejs') %>
        
        <div class="col-span-5 text-white">
            <h1 class="text-2xl p-5 ml-4"><%= title %></h1>
            <form action="/search" method="GET" class="ml-4">
                <input type="text" placeholder="Search Games" name="title" class="ml-5 pl-2 border-2 focus:border-amber-600 rounded-md focus:outline-none">
                <input type="submit" value="Search" class="ml-2 hover:cursor-pointer">
            </form>
            
        <!-- we'll have to map over all the games and display them. -->
        <div class="flex flex-wrap gap-5 items-center mt-9 ml-9">
            <% if (games.length === 0) { %>
              <p class="text-white text-xl p-6 w-[30%] text-center">No games found.</p>
            <% } else { %>
              <% games.forEach((game) => { %>
                <div class="border-4 border-gray-700 w-[30%] text-center flex flex-col gap-2 p-3">
                  <h2><%= game.title %></h2>
                  <p><%= game.description %></p>
                  <p><%= game.developer %></p>
                  <p>$<%= game.price %></p>
                  <div>
                    <button>
                        <a href="/edit/<%= game.id %>" class="border border-amber-600 px-6 py-1">Edit</a>
                      </button>
                      <button class="mt-2.5">
                        <a href="/delete/<%= game.id %>" class="border border-amber-600 px-4 py-1">Delete</a>
                      </button>
                  </div>
                  
                </div>
              <% }) %>
            <% } %>
          </div>
          
         
        </div>
        
    </div>
    
</body>
</html>